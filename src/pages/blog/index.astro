---
import { getCollection } from "astro:content";
import FormattedDate from "../../components/FormattedDate.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../../consts";
import Layout from "../../layouts/Layout.astro";

const posts = (await getCollection("blog")).sort(
    (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<Layout title={`${SITE_TITLE} — Blog`} description={SITE_DESCRIPTION}>
  <section class="mt-4 sm:mt-6">
    <ul class="space-y-4 sm:space-y-5">
      {
        posts.map((post) => (
          <li>
            <a
              href={`/blog/${post.id}/`}
              class="group inline-flex flex-col gap-1 no-underline"
            >
              <h2 class="font-heading text-lg leading-7 font-semibold text-foreground transition-colors duration-200 group-hover:text-primary sm:text-base sm:leading-normal">
                <span class="block font-mono text-xs text-primary sm:inline sm:text-inherit">
                  <FormattedDate date={post.data.pubDate} />
                </span>
                <span class="underline decoration-dashed underline-offset-4 transition-[text-decoration-style] duration-200 group-hover:decoration-solid sm:ml-2">
                  {post.data.title}
                </span>
              </h2>
            </a>
          </li>
        ))
      }
    </ul>
  </section>
</Layout>
